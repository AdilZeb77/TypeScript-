//Imortant Note
// typeScrit is a language where we define tyoes for the js Synax
// Declerations

// String Declerations
const name: string = "Alis";
console.log(name);

//if we try to set numer into strings we got an error msj from the language

const CNIC: string = 123456789;

//number declerarion
const id: number = 23;

//boolean
const isAge: boolean = true;

const age: undefined = undefined;

const data: null = null;

// if we try to pass other values into null and undefined then we got an error from the lang
